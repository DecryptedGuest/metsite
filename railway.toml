# railway.toml
[build]
builder = "RAILPACK"
buildCommand = "npm install && npx prisma generate"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && node server/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
